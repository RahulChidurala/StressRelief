<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hacklahoma.stressrelief.stressrelief">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/chill_app_launcher_round"
        android:label="ChillOut"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".UseCases.Menu.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UseCases.IndoorActivityMenu.IndoorActivity" />
        <activity android:name=".UseCases.IndoorActivities.IndoorBreathing.IndoorBreathingActivity" />
        <activity android:name=".UseCases.IndoorActivities.IndoorStretching.IndoorStrecthingActivity" />
        <activity android:name=".UseCases.StressRating.StressRatingActivity"/>
        <activity android:name=".UseCases.Graph.HappinessGraphActivity"/>
        <activity android:name=".UseCases.OutdoorActivities.OutdoorMenu.OutdoorMenuActivity"/>
        <activity android:name=".UseCases.OutdoorActivities.OutdoorParkActivity"/>
        <activity android:name=".UseCases.OutdoorActivities.OutdoorEventsActivity"/>
        <activity android:name=".UseCases.OutdoorActivities.OutdoorExerciseActivity"/>
        <activity android:name=".UseCases.IndoorActivities.IndoorSmileActivity"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
    </application>

</manifest>